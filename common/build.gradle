plugins {
    id 'dev.fabricmc.loom' version "${loom_version}" apply false
}
apply plugin: 'dev.fabricmc.loom'

dependencies {
    minecraft "com.mojang:minecraft:${minecraft_version}"
    mappings loom.officialMojangMappings()
}
